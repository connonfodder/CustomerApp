/*
package com.aadhk.customertest;

import com.aadhk.customer.BuildConfig;
import com.aadhk.customer.data.api.API;
import com.aadhk.customer.data.api.APIService;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.robolectric.RobolectricGradleTestRunner;
import org.robolectric.annotation.Config;


@RunWith(RobolectricGradleTestRunner.class)
@Config(constants = BuildConfig.class, sdk = 21)
public class APITest {
  */
/*  @Mock
    UserLoginPresenter presenter;

    @Mock
    UserLoginContract.View view;

    @Mock
    UserLoginModel model;*//*


*/
/*    @Before
    public void setUp() throws Exception{
        MockitoAnnotations.initMocks(this);
        presenter = new UserLoginPresenter();
        presenter.setVM(view, model);
    }*//*



    @Test
    public void addition_isCorrect() throws Exception {
//        List mockedList = mock(List.class);
//        mockedList.add("one");
//        mockedList.clear();
//        verify(mockedList).add("one");
//        verify(mockedList).clear();
        System.out.println("the Moment I can't breath");
        */
/*User body = new User("333@qq.com", "333");
        api.login(body).observeOn(Schedulers.io())
                .subscribe(new Subscriber<Response<User>>() {
                    @Override
                    public void onCompleted() {
                        System.out.println("--onCompleted---");
                    }

                    @Override
                    public void onError(Throwable e) {
                        System.out.println("--onError---" + e.toString());
                    }

                    @Override
                    public void onNext(Response<User> user) {
                        System.out.println("onNext" + user);
                    }
                });*//*
    }
}
*/
